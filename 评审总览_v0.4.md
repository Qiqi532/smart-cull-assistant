# 光影选片助手 / Smart Cull Assistant — 全栈评审总览（v0.4）

> 本文档为「全栈工程评审 + 优化」的汇总，供作者**复核确认**后决定是否提交 GitHub。
> 所有代码改动已落盘并通过验证；**未经作者确认不会 `git commit` / `git push`**。

---

## 0. 一句话结论

从「能跑的 Demo」到「可靠、可解释、可直接分发」的工具，本轮完成了三件大事：
1. **可靠性大修**：修复了 9 类真实会踩的坑（缓存自损坏、复杂度爆炸、连拍误删、资源泄漏、中文路径、闭眼漏判、缩略图污染原图等）。
2. **模型分析升级**：用实测数据证明原 BRISQUE 几乎无判别力，默认切到 `musiq`/`musiq-ava`，并实测提速。
3. **体验与分发**：前端做了专业化+人性化改造（浅色主题、评分可解释、快捷键、连拍保护、A/B 对比）；打包给出**真正自包含**的 onedir 方案——并澄清「没有 PowerShell」这个误判。

---

## 1. 可靠性问题 → 修复清单

| # | 问题 | 根因 | 修复 | 文件 |
|---|------|------|------|------|
| 1 | 首次运行/换版本后 CLIP 等模型**加载即崩**（OSError: Can't load image processor） | HuggingFace 缓存跨 transformers 版本残留 `.no_exist` 失效标记 + 不完整快照 | 新增 `models_guard.py`：缓存自愈（清失效标记/残缺快照，已修「仅匹配完整路径前缀」的 bug）、离线优先、镜像端点 `hf-mirror.com`、多级模型名回退链；加载失败抛带 `user_hint()` 的可操作错误 | `engine/models_guard.py`、`faces.py`、`quality.py` |
| 2 | 连拍 20 张**废 18 张** | 旧逻辑把「组内 Top2 之外」一律判废，那只是没赢、不是重复 | 仅与最佳帧 pHash 距离 ≤ `DUP_HAMMING_STRICT` 的真·重复才判废，落选者标 `is_similar_loser`（默认收起、可找回） | `pipeline.py`、`scorer.py`、`store.py` |
| 3 | 大目录分析**卡死/爆内存** | ① 组循环内反复重建 `idx_of_path`（O(G×N)）；② 组内 `next()/index()` 线性查找（O(组²)）；③ futures 持有全部解码 RGB | ① 映射循环外构建一次；② 改字典 O(1)；③ **分块流式**，任意时刻仅一块原图在内存（5000+ 张不爆） | `pipeline.py` |
| 4 | 点「取消」**不生效** | `as_completed` 后再 `cancel()` 对已执行任务无效，且 `ThreadPoolExecutor.__exit__` 会阻塞等全部完成 | 改为**分块提交 + 块间检查**，取消通常在 <2s 内生效 | `pipeline.py` |
| 5 | 连拍大批量人脸**内存/耗时泄漏** | 闭眼分类器分支每次 `detect_face_and_eyes` 新建 4 个 FaceMesh 实例且从不 `close()` | 复用 `_detect_landmarks()` 单例，零额外推理 | `faces.py` |
| 6 | 含中文路径下**人脸检测偶发崩** | MediaPipe 在首次 detect 调用时才初始化，顺序依赖导致中文路径下解析内置模型失败 | 改为**模块导入即 `_ensure_mediapipe()`** 完成 ASCII junction 准备 | `faces.py` |
| 7 | 闭眼检测**整体失效** | 旧逻辑 `if scene=="人像" and eyes_closed`，而 scene 来自 CLIP 零样本分类——`face_closed1.jpg` 被误判为「其他」(conf 0.410) | 解耦：**只要确实检测到人脸（`is_face`）即启用闭眼硬判** | `scorer.py`、`pipeline.py` |
| 8 | 缩略图**污染原始照片目录** | 旧版在用户照片目录内建 `.thumbs` | 改独立缓存目录 `config.THUMB_CACHE_DIR`（`.thumbcache`），不污染原图/同步盘 | `loader.py` |
| 9 | 缺「相似落败帧」概念 | 落选者被混进废片，摄影师平白丢素材 | 新增 `is_similar_loser` 列 + 折叠组（UI 可找回），与 `is_waste` 严格区分 | `store.py`、`pipeline.py`、`app_qt.py` |

**验证**：全部引擎模块 `py_compile` 通过；无模型依赖测试 42 passed（store/similarity/scorer 30 + quality 7 + faces 5）；模型重测试 `test_pipeline` + `test_e2e_smoke` passed；`is_similar_loser` 端到端 roundtrip 读回 = 1。

---

## 2. 模型分析（关键结论，均来自实测）

| 画质模型 | 单图耗时 | Cohen's d（好6/坏4） | 结论 |
|----------|----------|----------------------|------|
| BRISQUE（原默认） | 60ms | **≈ -19.26** | 仅对合成噪声有区分，**对真实照片几乎无判别力 → 降级为回退** |
| MUSIQ | 74ms | 4.08 | 可用 |
| **musiq-ava（新默认美学）** | 44ms | 3.59 | **更快且更准**（AVA 训练，贴合真实照片） |
| clipiqa | 93ms | 6.46 | 区分度最高但慢 |
| dbcnn | 39ms | 6.83 | 区分度高、最快 |

- **默认模型**：画质 `musiq`（回退 `dbcnn`→`brisque`），美学 `musiq-ava`（回退 `laion-head`→`clip-prompt`）。统一归一化 0–100 **越高越好**。
- **性能瓶颈消除**：原单图分析中 BRISQUE 占 **74%**（6 张图 3064/4450ms）。改为批量 GPU 推理（musiq batch）+ 流式流水线后该瓶颈消除。
- **LAION 美学头被质疑**：2026 北大论文实测其为「写实度测量器」而非美学，结论无法迁移到真实照片——故美学默认改用 AVA 监督的 `musiq-ava`。

---

## 3. 前端美化（专业化 + 人性化）

> 由前端子任务完成（`app_qt.py` + 新增 `styles.qss`），未改动 `engine/`。

1. **浅色专业主题**：`styles.qss`（冷静蓝绿强调色 + 中性灰底、圆角卡片、悬停态、可访问对比度）；加载失败回退内联 `FALLBACK_QSS`；移除旧 `DARK_QSS`。
2. **评分可解释性（为什么这个分数）**：`score_breakdown()`/`explain_html()` 把每张照片转成中文维度（清晰度/曝光/美学/人脸/场景/综合），缩略图悬停 tooltip + 「ⓘ 评分说明」弹层。
3. **人性化快捷键 + 图例**：0-5 标星 / P 保留 / X 排除 / A-D 选候选 / ←→·Tab 切组 / Esc 退出；**焦点在输入框时不触发**（防误标星）；页底常驻图例。
4. **连拍/序列保护**：组上下文标签（本组 N 张·推荐最佳帧·已保留 M 张）+ 「本组仅留最佳帧」按钮（**二次确认**后才标其余为排除，绝不静默清空整组）。
5. **人脸/闭眼徽章**：检测到人脸→蓝色「人脸」；闭眼→红色醒目「闭眼」；近似重复→橙色「近似重复」。
6. **is_similar_loser 独立可折叠组**：导出页/总览页默认收起的「近似重复落败帧」折叠组，每张带「找回」按钮（标记 P 进导出清单），主表默认不混入、不当废片。
7. **A/B PK 对比**：复审页「A/B 对比」按钮打开左右并排 + 各维度拆解对比。

**验证**：`py_compile app_qt.py` 通过；无头(offscreen)导入成功，构造完整 `MainWindow` 并驱动 reload/队列/总览/导出/`ScorePopover`/`CompareDialog` 均无异常。

---

## 4. 打包：去「脚本层」的真相与方案

**结论先行：原本就没有 PowerShell。** `start.bat` 是纯 BAT，`launcher.py` 是纯 Python。作者真正讨厌的「那层」是旧 `--onefile` exe 作为**启动器**仍需项目 `.venv` + 源码在场——并非 PowerShell。

本轮给出的**真正自包含**路径（新增文件）：
- `光影选片助手_dist.spec`：PyInstaller **onedir**，把 torch/transformers/PyQt6/mediapipe 等全部依赖打包进单文件夹；已用 `grep` 确认并排除未导入的 `timm/sklearn/scipy` 等。
- `build_dist.bat`：**纯 cmd.exe**（`@echo off`/`chcp`/`cd /d`/`call`/`if`/`set`/`tar`），无任何 PowerShell cmdlet。
- `dist_runtime_hook.py`：运行时把 HF/TORCH 缓存重定向到 exe 目录 `.hf_cache`/`.torch_cache`，默认走 `hf-mirror.com`，模型权重不落 C 盘、不打包（首次运行经镜像下载）。
- `installer.iss`：Inno Setup 安装包（桌面快捷方式 + 开始菜单 + 标准卸载，卸载清理模型缓存）。
- `README.md` 打包章节重写：方案 A 开发态启动器 / **方案 B 自包含 onedir（推荐分发）** / 方案 C Inno 安装包。

**验证**：spec 通过 `ast.parse`；`.bat` 全文无 PowerShell cmdlet；`installer.iss` 为标准 Inno 结构；被排除依赖确认未被 `app_qt.py`/`engine/` 导入。

---

## 5. GitHub 开源生态调研（可借鉴点）

调研 8 个同类项目，重点可落地借鉴：
- **facet**（215★）：9 维评分分解 tooltip + EXIF、连拍自动选优 + 序列保护 + per-face 徽章 —— 已在本项目前端落地。
- **pixcull**（105★）：导出必须做 **XMP/IPTC**，否则摄影师无法进 Lightroom/Capture One（断路）。→ **后续路线**。
- **PhotoSort**（21★）：`requirements`/`requirements-cuda` 双依赖文件（CPU/GPU 体积取舍）；windowed 下重定向 `stdout`（None 报错）。
- **imagededup**（5.7k★）/ **czkawka**（31.5k★）：感知哈希 + embedding 双路去重 + 多轮扫描缓存。
- **pianke**（~400-608★）：**A/B 擂台 PK**（组内方向键对决）比网格挑图决策负担更低 —— 已落地为 A/B 对比。
- **immich**（104k★）：时间轴 + 语义搜索浏览范式（背景参照）。
- **photocull-ai**（8★）：单 HTML + TF.js 零服务器极简分发（形态参照）。

**行业空白机会**：面向真实照片的轻量美学模型（SOTA 缺位）可作为本项目差异化亮点。

---

## 6. 测试与质量门禁

- 无模型依赖：store / similarity / scorer 30 passed，quality 7 passed，faces 5 passed。
- 模型依赖：`test_pipeline` + `test_e2e_smoke` passed（`test_faces` 因 v0.4 重构旧桩失效，已重写并 5 passed）。
- 测试随引擎语义变更同步更新：`test_scorer`（闭眼以 `is_face` 为准）、`test_quality`（`norm_quality` 0–100 越高越好）、`test_faces`（融合逻辑在更底层注入）。

---

## 7. 变更清单（待确认后提交）

**修改**：`engine/{__init__,config,faces,loader,pipeline,quality,scorer,similarity,store}.py`、`app_qt.py`、`tests/{test_faces,test_quality,test_scorer}.py`、`CHANGELOG.md`、`README.md`
**新增**：`engine/models_guard.py`、`styles.qss`、`光影选片助手_dist.spec`、`build_dist.bat`、`dist_runtime_hook.py`、`installer.iss`
**版本**：`engine/__init__.__version__` 0.2.0 → **0.4.0**

---

## 8. 后续路线（建议，未做）

1. **导出 XMP/IPTC**：对接 Lightroom/Capture One，否则摄影师无法回写星级（pixcull 教训）。
2. **ONNX 瘦身**：把 CLIP/美学头/闭眼分类器导出 ONNX，用 onnxruntime(~15MB) 替代 torch(~1GB)，解决体积/启动/杀软误报。
3. **闭眼检测升级**：MediaPipe 478 点含虹膜版 + EAR 做 head-pose 归一化；或裁眼部跑 ~100k 参数小 CNN。
4. **CPU/GPU 双分发**：默认 CPU 版，运行时检测 NVIDIA 再提示下载 GPU 加速包（PhotoSort 方案）。
5. **差异化亮点**：自研面向真实照片的轻量美学模型（行业空白）。

---

## 9. 提交门禁

以上为评审总览。**请作者确认是否提交。** 确认后我将：
1. `git add` 上述变更 → `git commit`（按本总览撰写提交信息）；
2. `git push` 到 `https://github.com/Qiqi532/smart-cull-assistant.git`；
3. 回报 commit 哈希。

如对某些改动有保留意见（例如不想把 `评审总览_v0.4.md` 提交进仓库、或想先只提交引擎部分），请直接说明。
