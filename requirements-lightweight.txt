# OpenCV lightweight edition: isolated, offline runtime and build/test tools.
# Create with: python -m venv .venv-light
# Install with: .\.venv-light\Scripts\python.exe -m pip install -r requirements-lightweight.txt

numpy>=1.26,<2
opencv-contrib-python>=4.8,<5
Pillow>=10,<12
# 0.10.35 removed the legacy solutions/FaceMesh API used by engine.faces.
mediapipe==0.10.21
imagehash>=4.3,<5
PyQt6>=6.8,<6.9
rawpy>=0.20,<1

# Local verification and packaging (excluded from the generated application).
pytest>=7,<9
pyinstaller>=6.16,<7
